rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Read-only from client, deny all writes
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
